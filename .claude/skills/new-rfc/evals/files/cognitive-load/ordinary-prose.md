The RFC is ready for review. It proposes one shared rule file and keeps host adapters thin.

The open choice is how a host loads the rule. Security and upgrade risks are listed with owners.

No code change is part of this RFC. Decide the load path next.
